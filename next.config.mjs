/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com','guiadacozinha.com.br','www.freepik.com',"imgs.search.brave.com"],
  }
};

export default nextConfig;
